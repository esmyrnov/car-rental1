server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/car_rental?useSSL=false&serverTimezone=UTC
    username: root
    password: 3131egor

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  thymeleaf:
    cache: false
